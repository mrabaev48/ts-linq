# Оставшиеся задачи для завершения проекта

## 📊 Текущий статус: 26/35 пакетов собрано (74%)

---

## ❌ Осталось исправить: 9 пакетов

### 1. **core** - 102 ошибки компиляции
**Проблема**: Несоответствие API между пакетами

**Что нужно исправить**:
- Добавить отсутствующие типы в @ts-linq/types:
  - `QueryPerformanceAnalysisOptions`
  - `QueryAnalysisInfo` 
  - `CircuitOpenError`
- Выровнять API интерфейсов:
  - `SqlLogger` - добавить методы: `queryStart`, `queryEnd`, `retry`, `circuit`
  - `RetryPolicy` - переименовать: `shouldRetryEx` → `shouldRetry`, `getDelayMs` → `getDelay`
  - `OrmMiddleware` - исправить сигнатуры `beforeExecute/afterExecute`
  - `ConnectionHealthCheckOptions` - добавить: `degradeAfterFailures`, `unhealthyAfterFailures`
  - `IndexMetadata` - добавить продвинутые свойства: `orders`, `expressions`, `collations`, `nulls`, `using`, `concurrently`, `withParams`, `mysqlVisibility`, `include`

**Оценка времени**: 2-3 часа

---

### 2. **cli** - 36 ошибок компиляции
**Проблема**: Импортирует классы миграций из @ts-linq/core вместо @ts-linq/migrations

**Что нужно исправить**:
- Изменить все импорты миграций:
  - `MigrationRunner` - из @ts-linq/migrations
  - `Migration` - из @ts-linq/migrations
  - `SchemaSnapshotBuilder` - из @ts-linq/migrations
  - `SchemaSnapshotSerializer` - из @ts-linq/migrations
  - `compareSchemas` - из @ts-linq/migrations
  - `generateMigrationFromDiff` - из @ts-linq/migrations
- Исправить API health check и fallback policy (см. core)

**Оценка времени**: 30-60 минут

---

### 3. **plugin-audit** - отсутствует tsconfig.json
**Проблема**: Нет конфигурации сборки

**Что нужно сделать**:
- Создать tsconfig.json (скопировать из другого плагина)
- Настроить references на зависимые пакеты
- Проверить imports в исходниках

**Оценка времени**: 15-30 минут

---

### 4. **plugin-multi-tenant** - отсутствует tsconfig.json
**Проблема**: Нет конфигурации сборки

**Что нужно сделать**:
- Создать tsconfig.json
- Настроить references
- Проверить imports

**Оценка времени**: 15-30 минут

---

### 5. **plugin-soft-delete** - отсутствует tsconfig.json
**Проблема**: Нет конфигурации сборки

**Что нужно сделать**:
- Создать tsconfig.json
- Настроить references
- Проверить imports

**Оценка времени**: 15-30 минут

---

### 6. **integration-nestjs** - не проверен
**Проблема**: Статус неизвестен

**Что нужно сделать**:
- Попробовать собрать
- Исправить ошибки компиляции (если есть)

**Оценка времени**: 30-60 минут

---

### 7. **e2e-tests** - не проверен
**Проблема**: Статус неизвестен

**Что нужно сделать**:
- Проверить конфигурацию Jest
- Убедиться что все тесты находят зависимости
- Запустить тесты

**Оценка времени**: 30-60 минут

---

### 8. **testkits** - не проверен  
**Проблема**: Статус неизвестен

**Что нужно сделать**:
- Попробовать собрать
- Исправить ошибки компиляции (если есть)

**Оценка времени**: 15-30 минут

---

### 9. **config** - построен, но возможны ошибки
**Проблема**: Был построен ранее, но мог сломаться после рефакторинга

**Что нужно сделать**:
- Пересобрать и проверить
- Исправить imports если необходимо

**Оценка времени**: 15 минут

---

## 📋 План действий

### Этап 1: Быстрые победы (1-2 часа)
1. ✅ Создать tsconfig.json для 3 плагинов
2. ✅ Собрать плагины
3. ✅ Исправить CLI импорты
4. ✅ Проверить testkits, config, integration-nestjs

### Этап 2: Исправление core (2-3 часа)
1. Добавить отсутствующие типы в @ts-linq/types
2. Выровнять API интерфейсов (SqlLogger, RetryPolicy, etc.)
3. Исправить IndexMetadata
4. Собрать core пакет

### Этап 3: Финальная интеграция (1 час)
1. Пересобрать все пакеты в правильном порядке
2. Запустить e2e тесты
3. Проверить что всё работает

---

## ⏱️ Общее время: 4-6 часов

---

## 🎯 После завершения

- ✅ Все 35 пакетов собраны без ошибок
- ✅ Тесты проходят
- ✅ Готово к публикации npm пакетов
- ✅ Готово к production использованию

