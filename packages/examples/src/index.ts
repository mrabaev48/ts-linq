// @ts-linq/examples
//
// Runnable programs demonstrating the ts-linq public API. Each file under `src/` is a
// standalone entry point — build once, then run any of them with `node`:
//
//   pnpm --filter @ts-linq/examples build
//   pnpm --filter @ts-linq/examples example:crud
//   pnpm --filter @ts-linq/examples example:linq-queries
//
// See ./README.md for prerequisites (a local PostgreSQL instance via the root
// `docker-compose.yml`).
export {};
