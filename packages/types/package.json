{
  "name": "@ts-linq/types",
  "version": "2.0.0-alpha.1",
  "private": true,
  "description": "Pure type definitions and interfaces for ts-linq (zero dependencies)",
  "main": "./dist/index.js",
  "module": "./dist/esm/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json && tsc -p tsconfig.esm.json",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
