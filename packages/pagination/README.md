# @ts-linq/pagination

> Pagination helpers for ts-linq.

Intended to provide standalone pagination utilities (offset/limit and keyset/cursor pagination
result shapes and helpers) reusable across the query layer and applications.

> **⚠️ Status: placeholder.** Currently a stub (`src/index.ts` only) with no declared dependencies.
> Version `2.0.0-alpha.1`. Note that pagination is **already implemented** inside `@ts-linq/query`
> (`PaginationBuilder`); this package's role needs to be decided. See `CLAUDE.md`.

## Package structure

```
src/
  index.ts   # placeholder
```

## License

Part of the ts-linq monorepo. See the repository root for license details.
